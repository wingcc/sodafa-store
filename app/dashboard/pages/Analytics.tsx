// Backward-compat wrapper — main implementation lives in ./Analytic/
// This file is kept so `import Analytics from './pages/Analytics'` still works.
'use client';
export { default } from './Analytic';
